# Milestone 01 — Repo İskeleti, README, Kurulum Rehberi ve Claude Güvenlik Ayarları

- **GATE:** `PASS`
- **Tarih:** 2026-08-30
- **Kapsam:** Yalnız geliştirme iskeleti ve insan/Claude çalışma ortamı.
  Ürün davranışı, RPC adapter'ı, invariant ve veritabanı mantığı **yazılmadı**.

---

## BASELINE

Hiçbir yazma yapılmadan önce:

```
$ git rev-parse --is-inside-work-tree   -> true
$ git status --short                     -> ?? .gitignore
                                            ?? CLAUDE.md
                                            ?? docs/
$ git diff                               -> (boş)
$ git log --oneline -5                   -> fatal: branch 'main' does not have any commits yet
$ shasum -a 256 "PDF document.pdf"       -> f2879abe…016  (Milestone 00 ile aynı)
```

- Git repository mevcut, branch `main`, **hiç commit yok**.
- `git init` **gerekmedi** ve çalıştırılmadı.
- `.superstack` dizini **yok**.
- `docs/milestones/00.md` → `GATE: PASS` doğrulandı; ön koşul sağlandı.

### Ön koşul olarak okunanlar

`CLAUDE.md`, `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, `docs/SECURITY.md`,
`docs/INVARIANTS.md`, `docs/SUPPORT_MATRIX.md`, `docs/PROTOCOL_SOURCE_LOCK.md`,
`docs/ROADMAP.md`, `docs/milestones/00.md` — tamamı okundu.

### Kaynak kilidi §10 zorunlu kontrolü — ACP-194 / Helicon

`avalanchego` `master` `upgrade/upgrade.go` yeniden okundu:

```
Mainnet HeliconTime: UnscheduledActivationTime
Fuji    HeliconTime: UnscheduledActivationTime
Somut tarihi olan en güncel upgrade: Granite (Mainnet 2025-11-19, Fuji 2025-10-29)
```

**Değişiklik yok.** ADR-0002 (`ACCEPTED_STATE_ASSURANCE`) geçerliliğini koruyor;
`PROTOCOL_SOURCE_LOCK.md` güncellenmesi gerekmedi.

---

## CHANGED

### Oluşturulan dosya ağacı

```
README.md                                 kök, ürün + kurulum + klasör tablosu
CONTRIBUTING.md                           milestone/diff/test/ADR/dependency düzeni
SECURITY.md                               açık bildirimi ve disclosure sınırı
package.json                              private workspace kökü
pnpm-workspace.yaml                       tek workspace kaynağı
tsconfig.base.json                        strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes
.nvmrc / .node-version                    24.20.0
.editorconfig
.prettierrc.json / .prettierignore
.dockerignore
.mcp.json.example                         opsiyonel GitHub MCP; token değeri YOK

.claude/settings.json                     takım guardrail'i
.claude/settings.local.json.example        kişisel override örneği
.claude/rules/security.md
.claude/rules/data-integrity.md
.claude/rules/testing.md
.claude/rules/apps.md
.claude/commands/milestone.md
.claude/commands/verify.md
.claude/commands/safety-audit.md
.claude/commands/handoff.md

docs/DEVELOPMENT.md                       prerequisites, Claude Code manuel kurulum, troubleshooting
docs/milestones/01.md                      bu rapor

apps/{cli,agent,api,console}/package.json + src/.gitkeep
packages/{domain,invariant-core,state-machine,config,evidence,testkit,
          rpc-quorum,ictt-adapters,storage-postgres,replay,alerts}/package.json + src/.gitkeep
config/{deployments,policies}/.gitkeep
infra/postgres/.gitkeep
scripts/verify-config.mjs                 çalışan config + güvenlik + katman kapısı
```

### Değiştirilen mevcut dosyalar — üçü de bu milestone'un zorunlu bağlantısı

| Dosya | Değişiklik | Neden zorunlu |
|---|---|---|
| `docs/SECURITY.md` | **§12 eklendi**: "Geliştirme ortamı: agent guardrail'leri sandbox değildir" | Prompt bunu açıkça `docs/SECURITY.md` içinde istiyor |
| `docs/ARCHITECTURE.md` | §7 monorepo yapısı teslim edilen ağaçla eşitlendi | Eski taslak (`web/`, `manifest/`, `ledger/`, `fixtures/`, kök `policies/`) teslimle **çelişiyordu**; iki kaynak-of-truth bırakılamazdı |
| `docs/DECISIONS.md` | D-12/D-13/D-14 + yeni "§3.1 Kullanıcıdan beklenen kararlar" + P-06/P-08/P-09 | Bu milestone'un kararları ve LICENSE/pin gibi bekleyen kullanıcı kararları |
| `.gitignore` | Kullanıcı içeriği **korunarak** genişletildi (credential biçimleri, `settings.local.json`, `.mcp.json`, DB data, evidence, test çıktıları) | Prompt `.gitignore`'un real env/credentials/DB/evidence/build'i dışlamasını istiyor |

**Hiçbir dosya silinmedi veya yeniden adlandırılmadı.** Milestone 00 dosyalarının satır
sayıları 00 raporundaki değerlerle birebir aynı kaldı (PRODUCT 202, INVARIANTS 254,
SUPPORT_MATRIX 106, PROTOCOL_SOURCE_LOCK 194, ROADMAP 188, DATA_MODEL 174,
TEST_STRATEGY 125, RESEARCH_SYNTHESIS 306, ADR'ler 97/136/127/120, CLAUDE.md 124).

`PDF document.pdf` SHA-256 **değişmedi**: `f2879abe…016`.

---

## VERIFICATION

| # | Komut | Exit | Sonuç |
|---|---|---|---|
| 1 | `git rev-parse --is-inside-work-tree` | 0 | `true` |
| 2 | `git status --short` / `git diff` | 0 | baseline kaydedildi |
| 3 | `claude --version` | 0 | `2.1.251 (Claude Code)` |
| 4 | `node --version` | 0 | `v26.0.0` (**pin dışı**, bkz. Q-02) |
| 5 | `pnpm --version` | 0 | `11.24.0` (**pin dışı**, bkz. Q-02) |
| 6 | `docker --version` | 0 | `29.7.2` |
| 7 | Node 24 LTS / pnpm 11.10.0 varlık kontrolü | 0 | `v24.20.0` LTS "Krypton"; pnpm `11.10.0` yayınlanmış |
| 8 | ACP-194 / Helicon yeniden kontrolü | 0 | Her iki ağda `UnscheduledActivationTime` — değişiklik yok |
| 9 | Resmî settings şeması indirildi | 0 | `json.schemastore.org/claude-code-settings.json`, 142 top-level key |
| 10 | `.claude/settings.json` şema uyumu | 0 | **Şemada olmayan field: YOK**; `defaultMode="default"` enum'da geçerli |
| 11 | `.claude/settings.local.json.example` şema uyumu | 0 | Şemada olmayan field: YOK |
| 12 | `node scripts/verify-config.mjs` | **0** | `verify:config OK - 15 workspace member(s), 0 failures` |
| 13 | Tüm JSON parse (20 dosya) | 0 | `parsed=20 failed=0` |
| 14 | `pnpm-workspace.yaml` içerik doğrulaması | 0 | `apps/*`, `packages/*` |
| 15 | `wc -l CLAUDE.md` | 0 | **124 < 200 → PASS** |
| 16 | Milestone 00 güvenlik kuralları CLAUDE.md'de duruyor mu | 0 | 12/12 anahtar ifade **OK** |
| 17 | `.gitignore` kapsama testi (17 yol) | 0 | Gerçek env/credential/DB/evidence/build **IGNORED** |
| 18 | `.gitignore` yanlış-dışlama testi (5 yol) | 0 | `.env.example`, `settings.json`, README, package.json **tracked** |
| 19 | `.dockerignore` simülasyonu (19 yol) | 0 | `dockerignore problems: 0` |
| 20 | Legacy `sentinel` identifier taraması | 0 | Yok (tek eşleşme `ICTT_SENTINEL_CONTROL_PLANE_TOKEN` = doğru canonical ad) |
| 21 | Ürün kodu kontrolü (`*.ts`/`*.tsx`/`*.js`) | 0 | **none** |
| 22 | Dependency / lockfile / node_modules kontrolü | 0 | **NONE** — hiçbir bağımlılık, lockfile veya lifecycle hook yok |
| 23 | `shasum -a 256 "PDF document.pdf"` | 0 | Milestone 00 ile aynı |
| 24 | `git diff --check` | 0 | temiz |

### Kapının gerçekten diş taşıdığının kanıtı — negatif testler

`.claude/settings.json` geçici olarak bozuldu, kapı çalıştırıldı, dosya her seferinde geri
yüklendi. **Altısı da doğru yakalandı:**

| Enjekte edilen hata | Exit | Yakalanan |
|---|---|---|
| `deny: Read(.env.*)` eklendi | 1 | `env-deny: ".env.example" must stay readable but is denied by Read(.env.*)` |
| `allow: Bash(pnpm *)` eklendi | 1 | `permission-allow: Bash allow "pnpm *" wildcards the whole subcommand space` |
| `defaultMode = "bypassPermissions"` | 1 | `claude-settings: permissions.defaultMode must be one of default/manual/plan` |
| `attribution.commit = "Co-Authored-By: Claude"` | 1 | `claude-settings: attribution must be {commit:"", pr:"", sessionUrl:false}` |
| `sandbox.autoAllowBashIfSandboxed = true` | 1 | `sandbox: ... else the narrow allowlist is bypassed` |
| `allow: Bogus(thing)` | 1 | `permission-rule: "Bogus(thing)" does not match the official permissionRule pattern` |

Geri yükleme sonrası temiz çalışma: `exit=0`.

> **Süreç notu:** İlk yazımda glob çeviricideki bir satır (`.replace(//g, …)`) JavaScript
> tarafından **yorum** olarak ayrıştırılıyordu; bu, üretilen regex'in sonunu anchor'sız
> bırakıyor ve `.env.example` çakışma testini **yanlış nedenle geçiriyordu**. Hata bulundu,
> çevirici yeniden yazıldı ve kapıya **12 vakalık matcher self-test'i** ile 8 vakalık naming
> self-test'i eklendi; artık bozuk bir matcher sessizce geçemez.

**`pnpm run verify` bu milestone'da mevcut değildir** (toolchain Milestone 02). Var olmayan
bir kapı "geçti" sayılmadı.

---

## ACCEPTANCE

| # | Kriter | Sonuç | Kanıt |
|---|---|---|---|
| 1 | Yeni geliştirici README + DEVELOPMENT ile repo amacını ve kurulumu anlayabiliyor | **PASS** | `README.md` (ne/neden/kim/modlar/klasör tablosu/quickstart/milestone düzeni), `docs/DEVELOPMENT.md` (prerequisites → kurulum → Claude Code → günlük komutlar → troubleshooting) |
| 2 | Claude global safety kuralları her çalışma alanında geçerli | **PASS** | `.claude/settings.json` (deny/ask/allow + fail-closed sandbox) + `.claude/rules/*` (4 dosya) + `CLAUDE.md` 124 satır, 12/12 kural korunmuş |
| 3 | Workspace mimari sınırları dosya yapısında açık | **PASS** | 15 workspace üyesi; her `package.json` `ictt-sentinel.layer` + `mayDependOn`; `verify:config` katman yönü ve döngüsüzlük denetliyor; README klasör tablosu |
| 4 | Secret, dependency veya ürün davranışı eklenmemiş | **PASS** | Doğrulama #21/#22: `*.ts` yok, dependency yok, lockfile yok, lifecycle hook yok, `.mcp.json.example` token **değeri** taşımıyor |
| 5 | Bütün config dosyaları parse ediliyor | **PASS** | Doğrulama #12/#13/#14: 20 JSON + workspace YAML, 0 hata |

### Prompt'un ek doğrulama listesi

| Talep | Sonuç |
|---|---|
| Tüm JSON dosyalarını parse et | **PASS** (#13) |
| Workspace glob ve paket isimleri tutarlı | **PASS** (#12) |
| Eski/genel `sentinel` adı kalmamış, canonical naming her yerde | **PASS** (#20) |
| CLAUDE.md satır sayısı ve `.claude` settings validity | **PASS** (#15, #10, #11) |
| `.gitignore`/`.dockerignore` real env, credentials, DB data, evidence, build çıktısını dışlıyor | **PASS** (#17, #18, #19) |
| Mevcut PDF ve idea-context dosyaları değişmemiş | **PASS** (#23; `.superstack` yok) |
| Ürün kodu eklenmediği `git diff` ile doğrulandı | **PASS** (#21, `git status` yalnız yeni iskelet yolları) |
| Permission sırası ve `.env.example` ↔ gerçek env deny çakışması resmî şemaya göre test edildi | **PASS** (negatif test 1; `deny > ask > allow` sırası resmî dokümandan doğrulandı) |

---

## DECISIONS

| ID | Karar |
|---|---|
| D-12 | Katman sözleşmesi makine-okunur: `ictt-sentinel.layer` + `mayDependOn`; aynı/daha düşük katman + döngüsüzlük; `domain` bağımlılık kökü |
| D-13 | `pnpm-workspace.yaml` tek workspace kaynağı; kök `package.json`'da `workspaces` alanı **yok** |
| D-14 | Claude izin modeli: `defaultMode="default"`, bypass ve auto **kapalı**, sandbox fail-closed, project MCP otomatik açılmaz, `attribution` boş |
| — | `git commit`/`git add`/`git tag` **`ask`** (deny değil): kullanıcı proje boyunca kendi adına commit istiyor; deny bunu imkânsız kılardı. `git push`, publish, `gh pr/release`, `docker push` **deny** |
| — | Resmî Claude Code şeması (`json.schemastore.org/claude-code-settings.json`) indirilip ayarlar ona karşı doğrulandı; **uydurma field veya permission mode yok** |
| — | Node `24.20.0` / pnpm `11.10.0` pinleri prompt'a göre yazıldı; kurulu ortam farklı olsa da pin **kendiliğinden değiştirilmedi** |
| — | LICENSE **varsayılmadı**: `package.json` → `UNLICENSED`, kökte `LICENSE` dosyası yok, karar `Q-01` olarak kaydedildi |
| — | `packages/config` (kod) ile kök `config/` (veri) ayrımı README, ARCHITECTURE ve `.claude/rules/apps.md`'de açıkça yazıldı |

---

## OPEN_RISKS

| ID | Konu | Durum |
|---|---|---|
| `Q-01` | **LICENSE seçilmedi** | Kullanıcı kararı bekliyor; `UNLICENSED` geçici |
| `Q-02` | **Node/pnpm pin sapması** — pin 24.20.0/11.10.0, kurulu 26.0.0 (LTS değil)/11.24.0 | Kullanıcı kararı bekliyor; Milestone 02'de `pnpm install` bu sapmayı yüzeye çıkaracak |
| `Q-03` | Repository görünürlüğü / GitHub remote'u | Remote yok, commit yok |
| `Q-04` | Sandbox ağ allowlist'i yalnız 3 domain | Docker image çekme veya ek registry gerekirse incelenerek genişletilmeli |
| — | `sandbox.failIfUnavailable: true` native Windows'ta oturumu **başlatmaz** | Bilinçli fail-closed; güvenli alternatif (WSL2/devcontainer) `docs/DEVELOPMENT.md` §3'te yazılı |
| — | `.claude/settings.json` **gerçek bir güvenlik sandbox'ı değildir** | `docs/SECURITY.md` §12'de açıkça yazıldı; production credential Claude sürecine verilmez |
| — | Şema CLI sürümünün gerisinde kalabilir | Resmî doküman bunu söylüyor; `claude doctor` + `/permissions` kullanıcı adımı olarak verildi |
| — | `verify-config.mjs` içindeki permissionRule pattern'i şemanın **pinlenmiş kopyasıdır** | Şema yeniden kontrol edilmeden güncellenmemeli (dosya başında not var) |
| — | Milestone 00'ın tüm `T01-T08` / `A01-A06` / `H01-H07` riskleri | Değişmedi; bu milestone protokol semantiğine dokunmadı |

---

## SAFETY

- Gerçek `.env`, credential store, shell history, process environment **okunmadı/yazdırılmadı**;
  kullanıcıdan secret istenmedi. Repository'de gerçek env dosyası **yok**.
- Private key, mnemonic, signer, wallet veya chain-write yüzeyi **oluşturulmadı**.
- `.mcp.json.example` yalnız env **referansı** (`${GITHUB_MCP_TOKEN}`) taşır, **token değeri yok**;
  `.mcp.json` `.gitignore`'a alındı ve project MCP otomatik açılmıyor.
- Ağ yalnız promptun istediği resmî kaynak okuması için kullanıldı: `avalanchego` `upgrade.go`,
  `json.schemastore.org` şeması, `code.claude.com` dokümanları, `nodejs.org` sürüm indeksi,
  npm registry sürüm sorgusu. **Remote script/eval çalıştırılmadı; hiçbir paket kurulmadı.**
- `sudo`, global install, `curl | sh`, uzaktan kod çalıştıran `npx/pnpx`, force flag,
  destructive delete **kullanılmadı**.
- Subagent, background task, watch mode, uzun yaşayan server **başlatılmadı**.
- **Git durum değiştiren hiçbir komut çalıştırılmadı** (`add`/`commit`/`push`/`init` yok).
- Negatif testler sırasında `.claude/settings.json` yedeklendi ve **her vakadan sonra geri
  yüklendi**; son durum doğrulandı (`exit=0`).
- Geçici dosyalar yalnız scratchpad dizininde; proje dizinine yazılmadı.

### Kullanıcı talimatı — commit

Kullanıcı proje boyunca kendi adına commit atılmasını istiyor. **PROMPT 01 commit'i açıkça
yasaklıyor** ("commit/push yapma"; ortak sözleşmede `git commit` yasak listesinde).
Milestone 00'da kullanıcı bu çelişkide **"prompt'a sadık kal"** demişti; aynı karar burada da
uygulandı ve **commit atılmadı**.

Buna karşılık talimat kalıcı olarak kurumsallaştırıldı: `git commit`/`git add`/`git tag`
`.claude/settings.json`'da **`ask`**tır (deny değil), ve `attribution` ile **hiçbir Claude
atfı** eklenmeyecek biçimde kilitlendi.

---

## BLOCKER

Yok.

`docs/ARCHITECTURE.md` §7'deki eski taslak yapı ile prompt'un hedef mimarisi çelişiyordu.
Bu, başka bir milestone'un işi değil, **bu milestone'un teslimini bağlayan** bir doküman
tutarsızlığıydı; en dar düzeltme (§7'nin teslim edilen ağaçla eşitlenmesi + klasör tablosunun
tek kaynak olarak README'ye işaret etmesi) yapıldı ve `CHANGED` bölümünde raporlandı.
Kapsam büyütülmedi.

---

## NEXT

`02_TOOLCHAIN_KALITE_KAPILARI_VE_CI.md` — **not started**

**STOPPED: yes**
