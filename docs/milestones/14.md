MILESTONE: 14 — Deterministik Failure Lab, Security Hardening ve Pilot Release Gate

GATE: PASS

BASELINE:

- Çalışma kökü `/Users/seyit/Desktop/ictt-sentinel`; başlangıç Git durumu temizdi.
- Başlangıç HEAD `e42ba5c`; `docs/milestones/13.md` içinde `GATE: PASS` doğrulandı.
- Gerçek `.env`, credential store, shell history, process environment ve kişisel anahtar okunmadı.

CHANGED:

- Fault lab 88 deterministik fixture'a genişletildi. Her fixture provenance ile
  sabit chain/block/hash/time/amount ve beklenen protocol/data/reason/exit/digest
  sözleşmesini taşır; async notifier failure da aynı tek komutta çalışır.
- RPC bağımsızlığı providerGroup ve trustDomain'in transitive ortak failure-domain
  ilişkisine göre sayılıyor. Genesis anchor, accepted-hash conflict, candidate
  block, message version/family ayrımı ve native minter ihlalleri kapıya bağlandı.
- API auth/body hostile-input ve evidence decoder property/fuzz testleri eklendi.
- PostgreSQL 1000 fact kaynak bütçesi; pure replay/latency/memory benchmark'ı;
  byte-for-byte reproducible build doğrulayıcısı eklendi.
- Supply-chain kapısı bütün pilot paketini checksum manifestine bağlar. SBOM root,
  image/service ve producer canonical `ictt-sentinel` adını kullanır.
- `docs/RELEASE_READINESS.md`, `docs/OPERATOR_QUESTIONNAIRE.md`, threat model ve
  pilot onboarding tamamlandı. Backup/restore ve incident runbook uygulanabilir.
- Gerçek restore tatbikatı `--bootstrap-roles` komutunun bakım veritabanını migrate
  etmeye çalıştığını buldu. Komut rol oluşturduktan sonra çıkacak şekilde düzeltildi.

VERIFICATION:

| Komut | Exit / sonuç |
|---|---|
| `pnpm install --frozen-lockfile` | `0`; lockfile güncel, indirme yok |
| `pnpm run clean` | `0` |
| `pnpm run test:unit` | `0`; 49 dosya, 1097 test |
| `ICTT_SENTINEL_TEST_DATABASE_URL=<ephemeral-local-test-dsn> pnpm run test:integration` | `0`; gerçek PostgreSQL, 9 dosya, 110 test |
| `ICTT_SENTINEL_TEST_DATABASE_URL=<ephemeral-local-test-dsn> pnpm run verify` | `0`; 59 dosya, 1214 test, 0 skipped/failure |
| `pnpm run lab` | `0`; 88 scenario, 7 gate assertion |
| `pnpm run test:e2e` | `0`; Playwright 10/10 |
| `pnpm run verify:supply-chain` | `0`; 220 paket sürümü, critical/high/moderate/low `0`, 0 finding |
| `pnpm run release:benchmark` | `0`; first evidence 2.45 ms, p95 0.17 ms, 10k replay 1009.86 ms, heap +0.52 MiB, RSS 70.27 MiB |
| `pnpm run verify:reproducible-build` | `0`; 639 dosya, iki build aynı `0ff492…468c4` digest |
| Üç CLI evidence export + `shasum -a 256` | `0`; 0.37/0.46/0.37 s, üçü aynı `290b04…29fa` file hash ve `4c14d4…af3b` content hash |
| `check --fixture deficit` | Beklenen exit `2`; `CRITICAL`, `COMPLETE`, `ACC-A01` |
| `check --fixture disagreement` | Beklenen exit `3`; `UNKNOWN`, `DIVERGENT`, `AGG-M04` |
| Agent `docker build --no-cache`, API/migrate target build | `0`; üç canonical image üretildi |
| Üç image non-root/read-only/cap-drop/no-new-privileges smoke | `0`; UID/GID `10001:10001`, yazma reddedildi |
| Üç image `.env`/key/keystore/PDF/private-evidence filename taraması ve image history incelemesi | `0`; eşleşme yok |
| Role bootstrap; migration iki kez; `pg_dump`; ayrı DB'ye `pg_restore`; migration sorgusu | `0`; `[1..8]` sonra idempotent, 91553-byte dump, restore'da 8/8 migration |
| `git diff --check` | `0` |

ACCEPTANCE:

- PASS — Beş zorunlu kalite sayacı sıfır.
- PASS — Full lint/typecheck/unit/property/integration/E2E/build gate çalıştı.
- PASS — Artifact ve evidence deterministik, checksum bağlı ve secret-free.
- PASS — SAST (`boundaries:check`), SCA, secret scan, SBOM ve lisans kapıları sıfır bulgu.
- PASS — Container base digest pinned; clean-build ve runtime hardening smoke geçti.
- PASS — Crash/restart/outbox, migration, backup/restore ve raw-fact projection rebuild doğrulandı.
- PASS — Pilot onboarding, auditor handoff, sharing/retention ve escalation sınırları yazılı.
- PASS — BLS/ICM non-goal ve accepted destination-state iddia sınırı açık.
- PASS — Otomatik deploy, chain write, signer, wallet ve autopause yüzeyi yok.

Fault lab sonuç tablosu:

| Aile | Scenario | Sonuç |
|---|---:|---|
| RPC / accepted-state / gap | 19 | PASS |
| Accounting / census / drift | 23 | PASS |
| Message lifecycle | 14 | PASS |
| Protocol / native | 17 | PASS |
| Evidence / API boundary / storage / alert | 15 | PASS |
| Toplam | 88 | PASS |

Beş zorunlu sayaç:

| Sayaç | Sonuç |
|---|---:|
| Required deterministic breach false negative | 0 |
| Gap/RPC/fingerprint/unsupported false OK | 0 |
| Same pinned input digest/verdict drift | 0 |
| Signing/write/autopause forbidden surface | 0 |
| Secret canary leak | 0 |

DECISIONS:

- RELEASE: TECHNICAL_PREVIEW.
- COMMERCIAL: VALIDATE.
- `SHADOW_PILOT_READY` dış kanıt olmadan verilmedi; teknik PASS bu kanıtların yerine geçmez.
- MVP accepted destination-chain state'ini izler. Historical P-Chain validator-set
  ve BLS aggregate signature/predicate doğrulaması release kapsamı dışıdır.
- Bootstrap-role ve application-migration iki ayrı operator adımıdır.

OPEN_RISKS:

- R1 — Gerçek deployment read-only erişimi ve gerçek config henüz repository kanıtı değil.
- R2 — İki providerGroup'un gerçek operasyonel bağımsızlığı operatörce doğrulanmalı.
- R3 — Gerçek deployment shadow replay/evidence ve üç pilot kontrollü demo bekliyor.
- R4 — Dört nitelikli operatör görüşmesi ve yazılı paid-pilot intent yok; ticari karar VALIDATE.
- R5 — Local bounded kaynak ölçümü production kapasite planının yerine geçmez.

SAFETY:

- Anahtar, signer, wallet, transaction, pause, deploy, publish, tag veya push yapılmadı.
- Yalnız ephemeral yerel PostgreSQL ve fictional offline fixture kullanıldı.
- Critical/high finding suppress/ignore edilmedi; risk acceptance kaydı gerekmedi.
- Tüm yerel commitlerin author/committer kimliği yalnız `sayweer <degirmenseyit@gmail.com>`.

BLOCKER: none for TECHNICAL_PREVIEW; SHADOW_PILOT_READY ve commercial GO için OPEN_RISKS R1-R4 dış kanıtları gerekir.

NEXT: none — prompt sequence complete

STOPPED: yes
