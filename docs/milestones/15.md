MILESTONE: 15 — Project presentation, CI recovery and release-document accuracy

GATE: PASS

BASELINE:

- Çalışma kökü `/Users/seyit/Desktop/ictt-sentinel`; başlangıç Git durumu temizdi.
- Başlangıç HEAD `903ade8`; branch `main`, remote `https://github.com/sayweer/ictt-sentinel.git`.
- Son GitHub Actions run'ında `verify:supply-chain`, `artifacts/sbom.cdx.json` ve
  `artifacts/licenses.json` dosyalarını stale bulduğu için başarısızdı. Önceki adımlar geçmişti.
- Kaynak incelemesi, ekonomik invariant/evidence motorunun offline çalıştığını; live `doctor`,
  discovery ve agent log replay'in bulunduğunu; canlı RPC state gözlemlerini ekonomik evidence
  girdisine dönüştüren uçtan uca collector'ın henüz bağlı olmadığını doğruladı.

CHANGED:

- `.github/workflows/ci.yml`, built console artefaktına karşı gerçek Chromium E2E testini ve
  byte-for-byte reproducible-build kontrolünü her push/PR kapısına ekledi.
- `README.md` uluslararası Avalanche geliştirici kitlesi için yeniden yazıldı: problem, değer,
  yetenekler, verdict/exit kodları, Mermaid mimari, güven modeli, offline quickstart, gerçek
  deployment shadow-pilot akışı, local servisler, destek sınırı, kalite kapıları ve dokümantasyon
  haritası tek giriş sayfasında toplandı.
- README, canlı ve offline yolları birbirine karıştırmadan technical-preview sınırını açıkça
  gösteriyor; example manifestin kurgusal olduğunu ve configured replay/export yolunun bugün
  pre-built evidence istediğini belirtiyor.
- `docs/DEVELOPMENT.md`, `docs/PILOT_ONBOARDING.md` ve `docs/SUPPORT_MATRIX.md` mevcut kodla
  eşitlendi. Eksik `--pins`, `--file` ve `--offline` örnekleri düzeltildi; Milestone 00'dan kalma
  “kod yok” statüleri kaldırıldı.
- Kök package metadata'sına güncel açıklama, repository, homepage ve issue bağlantıları eklendi.
- SBOM ve release checksum manifesti güncel metadata/dokümana göre yeniden üretildi; CI'ın
  görülen stale-artifact kök nedeni kapatıldı.
- `docs/DECISIONS.md` içindeki artık yanlış olan “remote yok; commit yok” kaydı güncellendi.

VERIFICATION:

| Komut | Exit / sonuç |
|---|---|
| Resmî Node `v24.20.0` Darwin arm64 arşivi için `shasum -a 256` | `0`; beklenen ve görülen `40e5607e…96bc8` |
| Pinli Node/Pnpm ile `pnpm run bootstrap:check` | `0`; Node `24.20.0`, pnpm `11.10.0` |
| `pnpm install --frozen-lockfile --ignore-scripts` | `0`; 220 exact lockfile paketi, lifecycle script yok |
| `pnpm run supply-chain:write` | `0`; SBOM/license/checksum artefaktları üretildi |
| `pnpm run verify:supply-chain` | `0`; 220 paket sürümü, critical/high/moderate/low `0`, 0 finding |
| `ICTT_SENTINEL_TEST_DATABASE_URL=<ephemeral-local-test-dsn> pnpm run verify` | `0`; 59 dosya, 1214 test; lint/typecheck/build/reproducibility PASS |
| `pnpm run lab` | `0`; 1 runner testi, 88 senaryolu fault corpus ve beş sayaç PASS |
| `pnpm run test:e2e` | İlk sandbox koşumu Chromium Mach-port izninde test başlamadan çevresel olarak kapandı; native browser izniyle tekrar `0`, 10/10 PASS |
| README local-link doğrulayıcısı | `0`; bütün repository-relative bağlantılar mevcut |
| `pnpm run format:check` | `0` |
| `git diff --check` | `0` |

ACCEPTANCE:

- PASS — CI kırmızısının kesin kök nedeni bulundu ve stale release artefaktları güncellendi.
- PASS — CI artık release belgesinin iddia ettiği browser E2E ve reproducible-build kapılarını
  gerçekten çalıştırıyor.
- PASS — README profesyonel, taranabilir, görsel hiyerarşili ve doğrudan uygulanabilir komutlar
  içeriyor.
- PASS — Dokümantasyon implemented/offline/pilot-validation sınırlarını doğru gösteriyor.
- PASS — Pinli toolchain altında cumulative test/security/build kapısı geçti.
- PASS — Commit author ve committer yalnız `sayweer <degirmenseyit@gmail.com>`.

DECISIONS:

- RELEASE etiketi `TECHNICAL_PREVIEW` olarak kalır. Live economic collector ve gerçek shadow-pilot
  kanıtı olmadan `SHADOW_PILOT_READY`, production-ready veya mainnet-ready denmez.
- Live economic collector, eksik causal evidence'i varsayımla `closed` sayacak dar bir patch ile
  taklit edilmedi. Böyle bir fail-open değişiklik ürünün `UNKNOWN` güven sözünü bozardı.
- LICENSE seçimi proje sahibinin hukuki kararıdır. Mevcut `UNLICENSED` durumu korunmuştur; Apache-2.0,
  MIT veya başka bir lisans kullanıcı kararı olmadan uygulanmadı.
- GitHub'a push yapılmadı. Hosted CI ancak bu yerel commitler gönderildiğinde yeni yeşil run
  üretebilir.

OPEN_RISKS:

- R1 — Daemon'ın canlı RPC state/log gözlemlerinden self-contained ekonomik evidence bundle
  üretmesi hâlâ teknik-preview açığıdır.
- R2 — Bir gerçek ICTT deployment, iki gerçekten bağımsız RPC trust domain, shadow replay/evidence
  ve üç kontrollü pilot demo hâlâ dış kanıt gerektirir.
- R3 — Repository `UNLICENSED` olduğu sürece kaynak görünürlüğü yeniden kullanım/dağıtım hakkı
  vermez; ekosistem contribution'ı için lisans seçilmelidir.
- R4 — GitHub Actions'ta mevcut kırmızı run tarihsel kalır; düzeltme commitleri push edilene kadar
  uzaktaki badge yeşile dönmez.
- R5 — Local doğrulama macOS'ta geçti; yeni Linux CI E2E kurulum adımının hosted runner sonucu
  ancak push sonrası görülebilir.

SAFETY:

- Private key, mnemonic, signer, wallet, chain-write, auto-pause, deploy veya publish yüzeyi
  eklenmedi.
- Gerçek `.env`, credential store, shell history veya kişisel anahtar okunmadı.
- Test için yalnız localhost'a bağlı, fixture parolalı ephemeral PostgreSQL container'ı kullanıldı
  ve koşumdan sonra durduruldu.
- Global kurulum yapılmadı. Pinli Node arşivi resmî `nodejs.org` kaynağından `/tmp` altına indirildi,
  resmî SHA-256 listesiyle doğrulandı ve sistem toolchain'i değiştirilmedi.
- Dependency sürümü veya lockfile değiştirilmedi; test silinmedi/skip edilmedi, strictness
  düşürülmedi ve advisory suppress edilmedi.
- Yerel commitler yalnız kullanıcı kimliğiyle atıldı; co-author veya araç atfı eklenmedi.
- Push, tag, release, publish, deploy veya chain transaction yapılmadı.

BLOCKER: none for this milestone. Remote CI status, license and real shadow-pilot evidence require
project-owner action.

NEXT: Owner selects a license, pushes the local commits, observes the hosted CI run, and supplies a
reviewed real deployment for the shadow-pilot gate. The live economic collector remains the next
substantial engineering milestone.

STOPPED: yes
