// @ictt-sentinel/storage-postgres
// PostgreSQL persistence for the append-only ledger and evaluations.
//
// Scaffold only: no product behaviour yet. The package name is exported so the
// module has a checkable public surface and the build graph is exercised.
export const PACKAGE_NAME = '@ictt-sentinel/storage-postgres' as const;
