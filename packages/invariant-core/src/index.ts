// @ictt-sentinel/invariant-core
// Deterministic invariant rules. Pure: no I/O, env, clock or randomness.
//
// Scaffold only: no product behaviour yet. The package name is exported so the
// module has a checkable public surface and the build graph is exercised.
export const PACKAGE_NAME = '@ictt-sentinel/invariant-core' as const;
