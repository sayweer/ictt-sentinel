{
  "//": "Editor and linter view: every TypeScript file, including tests and configs. Emits nothing. The build graph is tsconfig.build.json.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "types": ["node"],
    "baseUrl": ".",
    "//paths": "Mirrors the alias in vitest.config.ts. Files under tests/ are not members of any workspace package, so without this the linter sees @ictt-sentinel/* as `any` and every type-aware rule goes quiet. Build resolution is unaffected: tsconfig.build.json uses project references.",
    "paths": {
      "@ictt-sentinel/*": ["./packages/*/src/index.ts", "./apps/*/src/index.ts"]
    }
  },
  "include": [
    "apps/*/src/**/*.ts",
    "packages/*/src/**/*.ts",
    "apps/*/test/**/*.ts",
    "packages/*/test/**/*.ts",
    "tests/**/*.ts",
    "*.config.ts"
  ],
  "exclude": ["node_modules", "**/dist", ".tooling"]
}
