# Lifecycle scripts are reviewed before they are allowed to run (CLAUDE.md 6).
# Nothing in the current dependency set needs one; keep this closed by default.
enable-pre-post-scripts=false
# Refuse to silently proceed when the runtime does not match `engines`.
engine-strict=true
# Exact versions only; a caret range must never enter a manifest.
save-exact=true
